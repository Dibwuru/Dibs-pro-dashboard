ACT AS: A Senior Web3 Frontend Architect and Smart Contract Engineer.
TASK: Integrate live $DIBS ERC-20 token balance fetching using Viem and the authenticated Privy wallet address context.

CRITICAL TECHNICAL SPECIFICATIONS:

1. CONTRACT CONFIGURATION CONFIG:
   - Contract Address: '0x2b0ec237e5Cf460962E3eDe88cb676d83C807912'
   - Network Context: Arc Testnet (Chain ID: 5042002)
   - Minimal ERC-20 ABI Fragment to use for reading:
     [
       {
         "inputs": [{"name": "account", "type": "address"}],
         "name": "balanceOf",
         "outputs": [{"name": "", "type": "uint256"}],
         "stateMutability": "view",
         "type": "function"
       }
     ]

2. RETRIEVE USER EMBEDDED WALLET ADDRESS (src/app/page.tsx):
   - Access the active authenticated user context using Privy's 'usePrivy()' hook setup.
   - Safely capture the current wallet address string: 'const userAddress = user?.wallet?.address;'

3. REACTIVE ON-CHAIN BALANCE HOOK IMPLEMENTATION (src/app/page.tsx):
   - Utilize your project's active public client instance (Viem publicClient) or standard Wagmi 'useReadContract' hook configuration to query the 'balanceOf' function.
   - Pass 'userAddress' as the single argument input.
   - Set up active background polling intervals (e.g., watch: true or refresh every 8 to 10 seconds) to ensure that if a user receives $DIBS, the dashboard metric card auto-updates immediately without a manual browser page reload.

4. TEXT VALUE REPLACEMENT & DECIMAL FORMATTING:
   - Take the raw bigInt return from the blockchain and format it down safely into standard human readable decimals using Viem's 'formatUnits(balance, 18)' layout hook.
   - Replace the static placeholder text string "$0.00 / 0 DIBS" inside your main balance overview dashboard card block. Render the newly formatted live token state value cleanly inside your layout markup string.
   - Handle empty state gates: If 'authenticated' is false or data is loading, display standard fallback states smoothly.

5. VERIFY SYSTEM STABILITY AND DEPLOY:
   - Run 'npm run build' to clear Next.js compile trees and verify that no TypeScript or missing dependency breaks occur.
   - Execute production branch deployment sequence: git add ., commit with message "feat: link live on-chain $DIBS balance fetching engine via viem hooks", and push origin main.


Execute this smart contract backend synchronization update immediately.
