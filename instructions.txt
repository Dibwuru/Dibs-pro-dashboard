ACT AS: A Senior Web3 Frontend Architect.
TASK: Scrub all hardcoded Turnkey simulation remnants and force-render the active Privy auth button, Gas pill, and Theme toggle in the Navbar.

CRITICAL CORRECTION PROTOCOL:
1. WIPE SIMULATION GHOSTS: Open src/app/page.tsx and src/components/Navbar.tsx. Locate and completely delete the hardcoded string "bangzdk@gmail.com", along with any automatic console.log or useEffect statement tracking "Turnkey login initiated".
2. REFACTOR NAVBAR JSX: Open src/components/Navbar.tsx. Ensure the header layout explicitly returns these items on the right side of the navigation row, exactly adjacent to the standard "Connect Wallet" button:
   - The Theme Toggle Button (Sun/Moon icon switcher via next-themes).
   - The USDC Gas Status Pill (Displaying "Gas: -- USDC" or dynamic balance, with the tooltip/link to https://faucet.arc.io/).
   - The Privy Authentication Button: Render a button labeled "Sign In with Email" if 'authenticated' is false. Wire it explicitly to onClick={() => login()}. If 'authenticated' is true, display a truncated version of the logged-in user's email or account address with an onClick={() => logout()} action.
3. HYDRATION SAFE GUARD: Ensure all three elements are safely wrapped below your 'if (!mounted) return null;' statement to prevent server-side mismatches.
4. BUILD VALIDATION: Run 'npm run build' to verify that removing the old strings doesn't break any placeholder array states.
5. DEPLOYMENT PIPE: Run git add ., commit with message "fix: clear old auth simulation and force-render live privy navbar components", and push to origin main.

Execute this surgical cleanup and component render sync now.
