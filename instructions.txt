ACT AS: A Senior Web3 Developer.
TASK: Save our new frontend code changes and push them upstream to GitHub to trigger the Vercel production deployment.

CRITICAL REQUIREMENTS:
1. Run 'git status' to ensure our modifications to src/app/page.tsx are visible.
2. Run 'git add .' to stage and prepare all modified files for saving.
3. Run a git commit command with the exact message: git commit -m "feat: deploy hybrid layout and passive network banner UI"
4. Push the code securely to your main branch by running: git push origin main
5. Print a clear confirmation message to the terminal screen once the push succeeds so the Founder knows the code has been shipped.

Execute this deployment sequence now.
