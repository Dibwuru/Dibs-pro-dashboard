ACT AS: An Elite Solidity Smart Contract Security Auditor and Engineer.
TASK: Deploy a permanent, production-hardened Staking & Vault Contract on Arc Testnet with defensive state guards.

CONTRACT SPECIFICATIONS (Solidity):

1. MULTI-STAKE ARRAY STRUCT:
   - Define the stake data structure exactly as:
     `struct UserStake { uint256 amount; uint256 releaseTime; uint256 apyRate; bool claimed; }`
   - Map it globally: `mapping(address => UserStake[]) public userStakes;`

2. DEFENSIVE 'unstake(uint256 stakeIndex)' LOGIC:
   - RE-ENTRANCY & DOUBLE-DIP GUARD: 
     Fetch the stake: `UserStake storage stake = userStakes[msg.sender][stakeIndex];`
     Enforce: `require(!stake.claimed, "Already claimed");`
   - TIME-GATE LOCK: 
     Enforce: `require(block.timestamp >= stake.releaseTime, "Lock period has not expired yet");`
   - STATE FLIP FIRST: 
     Set `stake.claimed = true;` BEFORE executing any token transfers to secure the transaction state.

3. REWARD CALCULATION LINT:
   - Calculate rewards up to the releaseTime cap only:
     `uint256 reward = (stake.amount * stake.apyRate * lockDays) / (365 * 100);`
   - Execute the single combined mint/transfer token release payload to return (stake.amount + reward) to msg.sender.

DEPLOYMENT STEP:
1. Compile and verify code stability.
2. Deploy contract to Arc Testnet (5042002).
3. Replace the active 'VAULT_ADDRESS' string configuration inside 'src/vaultConfig.ts' with this updated deploy signature, preserving the named ABI inputs so frontend components never experience type-regressions.
4. Verify 'npm run build' outputs clean. Pushed directly to main.
