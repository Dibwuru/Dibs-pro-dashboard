ACT AS: A Senior Web3 Frontend Architect.
TASK: Clean up the frontend staking view cards to display the updated 5-field userStakes tuple data.

PRESERVATION LOCKS:
- Do NOT touch the newly deployed contract address (0xc45073b9de74c7f286c2545a618b703f31228cb6) or modify 'src/vaultConfig.ts'. 
- Retain all working Privy isUIActive layouts and native balance systems.

IMPLEMENTATION:
1. UPDATE UNSTAKE / ACTIVE STAKES DISPLAY (src/app/stake/page.tsx or Dashboard view):
   - Locate where the 'userStakes' contract data is being read and mapped to the UI.
   - Ensure the map index references match the new 5-field tuple return format:
     - stake: amount
     - stake: releaseTime
     - stake: apyRate
     - stake: lockDays
     - stake: claimed
   - Update the UI row component to render the real, historical 'apyRate' fetched directly from the tuple: `${Number(stake.apyRate)}% APY`.

2. REMOVE FIXED FUNDING WARNINGS:
   - Clean up any dead code, comment placeholders, or console logs regarding "funding the rewards reservoir" since the contract utilizes programmatic minting on maturity.

3. EXECUTE:
   - Run 'npm run build' to verify total component and data-mapping type-safety.
   - Git commit with message: "fix: map 5-field staking tuple to UI and clean up reward logs" and push to main.
