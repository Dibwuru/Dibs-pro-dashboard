ACT AS: A Senior Web3 Frontend Architect and Master UI/UX Engineer.
TASK: Polish the ARCTOR Terminal layout by implementing a responsive left hamburger menu drawer, fixing the scrolling navbar overlap bug, and correcting the dark mode text visibility contrast.

CRITICAL REFACTOR SPECIFICATIONS:

1. NAVBAR OVERLAP AND GLASS OVERLAY (src/components/Navbar.tsx):
   - Wrap the main outermost <nav> or <header> container inside these structural layout tokens: 'sticky top-0 z-50 w-full bg-white/80 dark:bg-[#090D16]/80 backdrop-blur-md border-b border-slate-200/80 dark:border-slate-800 transition-colors duration-300'. This creates an opaque visual gate keeping text from bleeding underneath during user scrolling.

2. MOBILE & DESKTOP LEFT HAMBURGER MENU DRAWER (src/components/Navbar.tsx):
   - Completely remove the plain center links ("Swap", "Stake", "Dashboard") from the desktop inline layout row.
   - On the far left of the Navbar (before the ARCTOR Terminal logo), insert a client-side state managed hamburger button component utilizing a simple React state variable: 'const [isOpen, setIsOpen] = React.useState(false)'.
   - Render a standard 3-line hamburger icon (or Lucide Menu icon). When clicked, it must slide open an elegant left side overlay container card containing your primary structural dashboard anchors: "Swap", "Stake", and "Dashboard".
   - Style the side drawer to match the Swiss Sovereign layout: 'fixed top-0 left-0 h-full w-64 bg-white dark:bg-[#121826] shadow-xl z-50 border-r border-slate-200/80 dark:border-slate-800 p-6'. Include an X close button.

3. TOP CONTROL HEADER CONSOLIDATION (src/components/Navbar.tsx):
   - Keep the right-side layout layout completely immaculate. It must cleanly align your key utility indicators side by side: [Gas Pill] -> [Theme Toggle Button] -> [Sign In with Email] -> [Wallet Address Pill] -> [Arc Testnet Badge] -> [Disconnect]. 

4. DARK MODE TEXT VISIBILITY REMEDIATION (src/components/Navbar.tsx & src/app/page.tsx):
   - Conduct a total typographical balance audit across both layout files.
   - For all major balance figures, headers, and dashboard titles, explicitly force high-contrast visibility by chaining light/dark modifiers: 'text-slate-950 dark:text-slate-50 font-bold'.
   - For subtext description strings, secondary metadata, and table subheadings, enforce: 'text-slate-500 dark:text-slate-400'. Ensure no text can render dark gray or black when the global HTML tree has the active '.dark' class name.

5. INTEGRITY VERIFY AND DELIVERY:
   - Run 'npm run build' to confirm no layout structure breaks, invalid close tags, or broken element scopes exist.
   - Perform repository sync code deployment: git add ., commit with message "ui: add left hamburger drawer, fix scrolling overlap backdrop blur, and restore dark mode text contrast visibility", and push origin main.

Execute this comprehensive aesthetic layout correction sweep now.
