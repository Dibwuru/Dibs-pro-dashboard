ACT AS: A Senior Web3 Full-Stack Engineer and DevOps Specialist.
TASK: Compile and deploy the upgraded 'DibsSwapVault.sol' contract to Arc Testnet, extract its new ABI/Address, and fully wire it to the user interface.

CRITICAL PIPELINE PROTOCOLS:

1. SMART CONTRACT COMPILATION & DEPLOYMENT:
   - Locate the saved 'DibsSwapVault.sol' file in '/root/dibs-pro/'.
   - Compile the contract using the project's established environment compilation tools.
   - Deploy the contract to the Arc Testnet (Chain ID: 5042002). Utilize or update the configuration settings found in the project's deployment configuration files or use the existing framework in 'scripts/deploy-vault.ts'.
   - Log and extract the newly generated deployment Contract Address and full ABI json map.

2. CONFIGURATION & ABI SYNCHRONIZATION:
   - Locate the frontend network/contract configuration files inside 'src/' (e.g., contract addresses file, constants file, or specific environment variables).
   - Update the placeholder address with the newly deployed 'DibsSwapVault' testnet address.
   - Replace the old contract ABI mapping with the new ABI containing the bi-directional swap, stake, and unstake function structures.

3. FULL FRONTEND WIREUP:
   - BI-DIRECTIONAL SWAP: Wire the UI toggle arrow. Connect the 'swapUsdcForDibs' function for the entry route. Connect the brand-new 'swapDibsForUsdc' function for the exit route, ensuring input values pass correctly.
   - STAKE LOCKUP VALIDATION: Bind the 7/30/90/180-day selection pills to pass the chosen duration variable directly into the contract 'stake(amount, lockDays)' parameters. Ensure the warning confirmation modal triggers before execution.
   - UNSTAKE SUPPORT: Build out or activate the Unstake panel/tab interface. Read the user's active stakes using the 'userStakes' mapping or 'getUserStakesCount' view function, and wire up the 'unstake(stakeIndex)' call to execute withdrawals for matured tokens.

4. PRODUCTION VERIFICATION:
   - Run 'npm run build' to ensure flawless frontend compilation against the newly mapped Web3 definitions.
   - Git commit with message: "feat: deploy upgraded DibsSwapVault and completely wire bi-directional swap, staking tiers, and unstake execution" and push to main.
