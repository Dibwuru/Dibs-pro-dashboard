ACT AS: A Principal Web3 Full-Stack Architect and UI/UX Designer.
TASK: Implement a dynamic custom token registry asset tracker, wire the frontend Swap interface to the deployed Vault, and integrate live SVG QR rendering for the Receive modal.

CRITICAL IMPLEMENTATION SPECS:

1. DYNAMIC TOKEN REGISTRY ENGINE (src/app/page.tsx):
   - Refactor the dashboard asset display matrix to read from a reactive state array named 'tokenList'.
   - INITIAL ASSETS CONFIGURATION: Pre-populate the registry with standard known entities on Arc Testnet:
     * Native Gas Asset: Name: "USDC Gas", Decimals: 6, Address: "Native"
     * Platform Utility Asset: Name: "DibsCoin", Symbol: "DIBS", Decimals: 18, Address: "0x2b0ec237e5Cf460962E3eDe88cb676d83C807912"
   - IMPLEMENT IMPORT FUNCTIONALITY: Add a clean visual text input form row labeled "Import Custom Token Address".
   - When a user inputs an arbitrary 0x token contract address and clicks "Import":
     * Use Viem's multicall or publicClient to dynamically query standard ERC-20 read functions: 'symbol()', 'decimals()', and 'balanceOf(userAddress)'.
     * Gracefully append this newly discovered token object metadata into the 'tokenList' UI rendering state array so it updates the dashboard instantly.

2. SWAP CONTRACT WIRING INTEGRATION:
   - Target Swap Vault Address: 0x3ed226184b4a00d1500e04f4fa89281107475597
   - Fixed Exchange Ratio: 1 USDC = 10 $DIBS
   - Hook up the input metrics: typing a USDC value must update the target $DIBS output card automatically (Input * 10).
   - Wire the "Execute Swap" action button to trigger an on-chain contract write calling 'swapUsdcForDibs()' [payable], transmitting the user's typed USDC amount as the active 'value' parameter payload ('value: parseUnits(usdcInput, 6)').

3. INSTALL & RENDER LIVE QR CODES:
   - Run 'npm install react-qr-code' to bring in a lightweight type-safe SVG rendering library.
   - Open your dashboard Receive modal layout container.
   - Import QRCode from 'react-qr-code'.
   - Swap the static placeholder row out with:
     <QRCode value={user?.wallet?.address || ""} size={180} fgColor="#D4AF37" bgColor="transparent" style={{ height: "auto", maxWidth: "100%", width: "100%" }} />
   - Pass the full un-truncated 'user?.wallet?.address' string string directly into the value property.

4. BUNDLE TESTING & PRODUCTION DELIVERY:
   - Run 'npm run build' to clear cache and guarantee zero compilation, type-mismatch, or module registry breaks.
   - Execute production branch push: git add ., commit with message "feat: complete automated swap engine, live address qr code generator, and custom erc20 asset import registry", and push origin main.

Execute this comprehensive full-stack token mechanics and layout loop immediately.
