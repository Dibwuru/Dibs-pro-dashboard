ACT AS: A Senior Web3 React Developer.
TASK: Rewire the existing Web3 state and logic hooks back into the newly merged UI components.

CRITICAL REWIRING PROTOCOLS:
1. AUTHENTICATION BINDING:
   - Locate the 'Connect Wallet' and 'Sign In' buttons in the new landing page and Navbar components.
   - Reattach the Privy 'login()' and 'logout()' execution hooks to these specific buttons' onClick handlers.

2. INPUT & STATE REATTACHMENT:
   - In the Swap and Stake views, ensure the user's live token balances are successfully passed into the new UI text displays.
   - Bind the new text input fields (where users type amounts) back to the React state variables that track input values.

3. TRANSACTION EXECUTION:
   - Reattach the Viem transaction calls to the primary 'Swap' and 'Stake' submission buttons.
   - Ensure the 'waitForTransactionReceipt' logic correctly triggers the new UI loading spinners or success states.

4. BUILD & DEPLOY:
   - Run 'npm run build' to verify all hooks are correctly imported and bound.
   - Commit and push to main with message: "fix: rewire Viem and Privy logic to new designer UI components".
