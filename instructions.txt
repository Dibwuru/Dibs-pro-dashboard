ACT AS: A Senior Web3 Frontend Engineer.
TASK: Add an interactive, hydration-safe Light/Dark mode toggle button into the ARCTOR Terminal Navbar.

CRITICAL CODE REQUIREMENTS:
1. COMPONENT TARGET: Open src/components/Navbar.tsx. Import 'useTheme' directly from 'next-themes'.
2. HYDRATION MOUNT CHECK: Inside the Navbar component, implement a client-side mount state to completely eliminate React compilation mismatches:
   const [mounted, setMounted] = React.useState(false);
   React.useEffect(() => setMounted(true), []);
   if (!mounted) return null;
3. THE TOGGLE ELEMENT: Insert a styled clickable <button> element immediately adjacent to the green Arc Testnet badge and the USDC Gas pill layout row.
4. INTERACTIVE MECHANICS: Configure the button's onClick event handler to check the active theme state:
   onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}
5. VISUAL ACCENTS: Render a clean Lucide icon layout inside the button (use Sun icon when theme is dark, Moon icon when theme is light). Style the button container with crisp, institutional borders: 'p-2 rounded-lg bg-white dark:bg-[#121826] border border-slate-200/80 dark:border-slate-800 text-slate-800 dark:text-amber-400 hover:scale-105 transition-all'.
6. DEPLOYMENT PIPELINE: Run 'npm run build' to confirm a successful compilation. Execute a clean repository save sequence: git add ., git commit -m "feat: add interactive theme toggle button to navbar", and git push origin main.

Execute this component refactor and manual push pipeline now.
