ACT AS: A Senior Web3 Frontend Architect.
TASK: Fix the dead Disconnect buttons, remove the false-positive network warnings, and sync the Navbar gas balance with the Dashboard native balance.

CRITICAL BUG FIXES:

1. FIX THE DEAD DISCONNECT BUTTONS (Navbar.tsx & Sidebar.tsx):
   - You MUST bind an 'onClick' handler to the Disconnect buttons/icons in BOTH the Navbar and Sidebar components.
   - Implement this exact function inside both components:
     ```typescript
     const { logout } = usePrivy();
     const { wallets } = useWallets();
     const handleHardDisconnect = async () => {
         if (wallets) await wallets.disconnect();
         await logout();
         localStorage.clear();
         window.location.reload();
     };
     ```
   - Bind `onClick={handleHardDisconnect}` to the disconnect elements. Do not leave them as dead UI.

2. FIX FALSE-POSITIVE NETWORK WARNINGS (src/app/page.tsx & Dashboard Layout):
   - The UI is showing "You are connected to an unsupported network" even when Wagmi successfully reads the Arc Testnet balance.
   - Update the network warning logic to correctly check BOTH the Wagmi 'useChainId()' and the active Privy wallet's chain. 
   - Ensure the expected chain ID is strictly '5042002'. If Wagmi successfully returns the native USDC balance, suppress the warning.

3. SYNC NAVBAR GAS BALANCE (Navbar.tsx):
   - The Navbar currently shows 'Gas: <0.0001 USDC' while the Dashboard correctly shows '104.37 USDC'.
   - Update the Navbar component to use the EXACT same native balance fetching hook (e.g., Wagmi's 'useBalance' targeting the active address) that the Dashboard is using.
   - Format the Navbar display to match: `Gas: ${formattedBalance} USDC`.

4. EXECUTE:
   - Run 'npm run build' to confirm no type errors.
   - Git commit with message: "fix: bind hard disconnect to buttons, resolve false network warnings, and sync navbar gas balance" and push to main.
