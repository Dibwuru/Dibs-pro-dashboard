ACT AS: A Senior Web3 Architect specializing in Privy integration.
TASK: Restore independent, dynamic wallet routing so both Google/Email embedded wallets and external MetaMask wallets can execute transactions and read balances without conflicting.

CRITICAL ARCHITECTURE FIX:
The application is currently hardcoding transaction requests to 'window.ethereum' or an injected Wagmi connector, completely breaking functionality for users who log in via Google/Email (Privy Embedded Wallets). 

EXACT IMPLEMENTATION:

1. DYNAMIC PROVIDER RESOLUTION (src/app/swap/page.tsx, src/app/stake/page.tsx, & UI Components):
   - You MUST remove any hardcoded references to the injected MetaMask provider for transactions and balance reading.
   - Import 'useWallets' from '@privy-io/react-auth'.
   - Extract the active wallet: 'const { wallets } = useWallets(); const activeWallet = wallets;'
   - For ALL balance reads (USDC and DIBS) and ALL contract writes (Swap and Stake), you MUST retrieve the provider dynamically:
     'const provider = await activeWallet.getEthereumProvider();'
   - Build your Viem PublicClient and WalletClient using THIS specific 'provider'. This ensures that if a user logs in with Google, the embedded wallet handles the swap. If they connect MetaMask, MetaMask handles it.

2. BALANCE SYNCING:
   - Ensure the native USDC balance hook specifically queries 'activeWallet.address'. Do not let it default to a disconnected Wagmi state.

3. EXECUTE:
   - Run 'npm run build' to confirm clean compilation.
   - Git commit with message: "fix: implement dynamic provider resolution to support independent Google embedded wallets and MetaMask routing" and push to main.
