ACT AS: A Senior Frontend Developer & Tailwind CSS Expert.
TASK: Fix the text clipping and overlapping token labels on the Swap and Stake input fields across page.tsx, swap panels, and stake panels.

CRITICAL VISUAL FIX REQUIREMENTS:
1. RELATIVE BOUNDARY LOCK: Locate all token input fields where "ETH" or "DIBS" badges are rendered inside the Swap and Stake forms. Ensure the wrapping parent element explicitly has the 'relative' Tailwind CSS utility class applied.
2. INPUT PADDING ROOM: On the <input /> elements themselves, add deep right-hand interior padding using the 'pr-16' or 'pr-20' utility classes. This prevents typed numbers from sliding under or overlapping the token text badges.
3. INSIDE BADGE BOXING: Style the position of the absolute token labels ("ETH" and "DIBS") using exactly 'absolute right-3 top-1/2 -translate-y-1/2'. Add a premium badge background container using 'bg-slate-900/90 px-2.5 py-1 rounded-lg border border-slate-800' so they sit perfectly locked INSIDE the input border walls.
4. CONTINUITY: Keep the global Obsidian Black background theme, spacing layout grids, and active wallet connection states entirely intact.

Execute this layout boundary alignment and input padding fix now.
