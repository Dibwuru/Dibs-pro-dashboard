ACT AS: A Principal Web3 Full-Stack Architect and Elite UI/UX Developer.
TASK: Implement the sidebar profile layout refactor, add 50%/MAX input shortcuts, cap the live activity history stream, and complete all remaining transaction notification linkages.

CRITICAL ARCHITECTURAL SPECIFICATIONS:

1. SIDEBAR PROFILE RESHAPE & BOTTOM PINNING (src/components/Sidebar.tsx):
   - PIN TO BASELINE: Restructure the left navigation panel layout to anchor the user identity card perfectly at the very bottom of the sidebar plane.
   - SWISS SOVEREIGN GLASSMORPHISM: Apply a premium border-accented glassmorphic container style (thin translucent borders, dark blurred backdrop, white and gold text accents).
   - TRUNCATE IDENTITY TEXT: Instead of displaying the raw, full email string (like 'diburotech@gmail.com'), implement a dynamic text trimmer that display it cleanly (e.g., 'dibu...tech').
   - ALPHABET AVATAR SIGN: Ensure the profile icon circle renders a bold, capitalized first letter of the user's email (e.g., a gold "D") centered inside a high-end gold-ringed circle.

2. BUILD THE USER PROFILE CONFIGURATION MODAL:
   - Wire a state toggle 'isProfileEditorOpen' to fire when a user clicks directly on the sidebar's Alphabet avatar circle.
   - RENDER THE PROFILE EDIT WINDOW: Create a glassmorphic overlay sheet featuring:
     * Text inputs for "Display Name / Handle" and "Custom Bio Details".
     * An image URL text field for "Drop Custom PFP Image URL".
     * A "Save Profile Settings" button that triggers a 'toast.success("Identity settings updated successfully!")' alert, updates local view states, and closes the modal.

3. INTEGRATE "50%" & "MAX" BALANCE SHORTCUTS (src/app/page.tsx):
   - Embed small, clickable gold "50%" and "MAX" action items directly adjacent to the input fields on the Swap module page and inside the Send/Stake modals.
   - DIBS TOKEN SHORTCUTS: Clicking "50%" fills the input with (dibsBalance * 0.5); clicking "MAX" fills it with the total live 'dibsBalance'.
   - USDC GAS SHORTCUTS: Clicking "50%" fills the input with (gasBalance * 0.5); clicking "MAX" fills it with (gasBalance - 0.01) to dynamically maintain a protective safety buffer against account gas starvation.

4. HYGIENE CLAMP FOR RECENT ACTIVITY FEED (src/app/page.tsx):
   - Locate the data loop displaying your live on-chain contract logs via Viem.
   - Apply a JavaScript '.slice(0, 10)' limit array boundary constraint on the incoming events data array before it renders to the DOM. This ensures the UI only tracks the 10 most recent transactions to safeguard long-term browser rendering performance.

5. REINFORCE SWAP PAGE NOTIFICATIONS:
   - Ensure the 'swapUsdcForDibs' execution pipeline on the swap interface is tightly bound to Sonner's 'toast.promise()' lifecycle hooks, firing instant loading state messages, block-confirmation successes, and explicit gas error logs.

6. PRODUCTION SYSTEM COMPILE & SHIP:
   - Run 'npm run build' to clear compilation environments and guarantee a 100% flawless build loop with zero TypeScript variations.
   - Deploy production code: git add ., commit with message "feat: launch absolute final polish sweeping sidebar user capsules, input shortcuts, and event stream caps", and push origin main.

Execute this comprehensive full-stack engineering and layout refinement sweep now.
