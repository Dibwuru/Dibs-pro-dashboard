ACT AS: A Principal Web3 Core Systems Engineer and Full-Stack Architect.
TASK: Integrate programmatic chain switching for Privy embedded wallets, fix Viem log topic padding for the Recent Activity tracker, and ensure multi-viewport transactional safety.

CRITICAL CODE MODIFICATIONS:

1. DEPLOY AUTOMATED PRIVY CHAIN-SWITCHING GUARD (src/app/page.tsx & src/app/swap/page.tsx):
   - Locate the submission event handlers for BOTH the Swap execution action and the Send asset modal.
   - Extract the primary user wallet array utilizing Privy's native 'useWallets()' hook.
   - MANDATORY NETWORK VALIDATION: Before instantiating any custom Viem 'createWalletClient' instances, execute an automated network verification:
```typescript
     const { wallets } = useWallets();
     const activeWallet = wallets[0];
     
     if (activeWallet) {
       const currentChainId = Number(activeWallet.chainId.replace('eip155:', ''));
       if (currentChainId !== 5042002) {
         // Programmatically forces the Privy embedded wallet to shift from Ethereum (1) to Arc Testnet (5042002)
         await activeWallet.switchChain(5042002);
       }
     }
     ```
   - Once the network targets match, route the provider context smoothly into your custom Viem client instances to sign transactions cleanly under your existing Sonner 'toast.promise()' lifecycles.

2. REFIX AUTOMATED TOPIC PADDING FOR LIVE ACTIVITY FEEDS (src/app/page.tsx):
   - Locate your background block scanner inside the Recent Activity panel layout.
   - OBLITERATE MANUAL HEX STRING FILTERS: Do not pass unpadded address strings directly into the raw topics filter array. Instead, pass your variables using Viem's native 'args' compilation parameters targeting the $DIBS contract:
```typescript
     const logs = await publicClient.getLogs({
       address: '0x2b0ec237e5Cf460962E3eDe88cb676d83C807912',
       event: parseAbiItem('event Transfer(address indexed from, address indexed to, uint256 value)'),
       args: {
         // Viem handles standard 32-byte hexadecimal padding for these parameters natively
         from: userAddress,
         to: userAddress
       },
       fromBlock: 'earliest'
     });
     ```
- Slice the output cleanly using '.slice(0, 10)' to preserve DOM frame rendering performance, and map the items to your activity layout.

3. PRODUCTION SYSTEM VERIFICATION:
- Execute 'npm run build' to clear compilation pipelines and confirm 100% type safety with zero linting or scope errors.
- Ship to production: git add ., commit with message "fix: implement automated embedded wallet chain switching and resolve log address topic padding parameters", and push origin main.

Execute this comprehensive full-stack blockchain interaction repair loop immediately.
