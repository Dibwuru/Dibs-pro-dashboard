ACT AS: A Senior Web3 Frontend Architect.
TASK: Resolve the final three follow-up items on the /dashboard view to achieve 100% feature harmony.

IMPLEMENTATION:

1. WIRE STAKE MODAL (src/app/dashboard/page.tsx):
   - Locate the 'Stake' quick-action button in the dashboard view.
   - Mirror the working modal pattern: import 'StakeModal' if it exists, establish a 'showStakeModal' state toggle, and bind it to the button's onClick event. 

2. BIND CARD DISCONNECT (src/app/dashboard/page.tsx):
   - Locate the red 'Disconnect' button inside the main dashboard portfolio metric block.
   - Ensure it imports and implements the absolute hard reset handler to clear wagmi/privy cache and force reload:
     ```typescript
     const { logout } = usePrivy();
     const { wallets } = useWallets();
     const handleHardDisconnect = async () => {
         if (wallets) await wallets.disconnect();
         await logout();
         localStorage.clear();
         window.location.reload();
     };
     ```
   - Bind `onClick={handleHardDisconnect}` directly to that element.

3. CLEAN UP ANIMATION CLASSES:
   - Search the project files for any lingering instances of the old class 'activity-row-new'.
   - Globally replace 'activity-row-new' with our working Tailwind class 'animate-fade-in' to ensure all item updates animate correctly.

4. DEPLOY:
   - Run 'npm run build' to ensure no hanging modal props cause compilation errors.
   - Git commit with message: "fix: finalize dashboard modal triggers, card disconnect click, and animation class unification" and push to main.
