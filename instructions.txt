ACT AS: A Lead UI/UX Frontend Developer.
TASK: Fix the horizontal and vertical alignment of the main content areas in the application so they sit perfectly centered within the viewport.

CRITICAL LAYOUT CORRECTIONS:
1. CENTER THE AUTH GATEWAY (Landing Page):
   - Locate the unauthenticated landing page view (the Arctor Terminal login card) in 'src/app/page.tsx'.
   - Update its outermost wrapper div to use strict Flexbox centering. It must occupy the full available height and center its children.
   - Required Tailwind classes for the wrapper: 'flex flex-col items-center justify-center min-h-[80vh] w-full'.

2. CENTER THE DASHBOARD CONTENT:
   - Locate the authenticated dashboard view (where the Balance, Staked, Sent cards are displayed).
   - The content is currently hugging the left edge. Wrap the main dashboard content (Title, Grid cards, Recent Activity) inside a constrained, centered container.
   - Required Tailwind classes for the wrapper: 'max-w-6xl mx-auto w-full px-4 sm:px-6 lg:px-8'.
   
3. VERIFICATION & DEPLOY:
   - Do NOT alter any Web3 logic, Privy hooks, or Viem configurations.
   - Run 'npm run build' to ensure zero errors.
   - Git commit with message: "ui: align landing page and dashboard containers for perfect center positioning" and push origin main.
