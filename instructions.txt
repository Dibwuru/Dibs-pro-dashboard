ACT AS: Senior Next.js DevOps Engineer
TASK: Update vercel.json to include standard SPA rewrite rules to fix the direct page 404 errors on Vercel deployment domains.

IMPLEMENTATION:
Modify 'vercel.json' so it retains the existing "headers" block, but add a "rewrites" block right below it to route all page requests to the root index layout.

The updated vercel.json structure must look exactly like this:
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Security-Policy",
          "value": "frame-ancestors 'self' https://auth.privy.io https://*.privy.io; frame-src 'self' https://auth.privy.io https://*.privy.io;"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/((?!api/|static/|_next/|favicon.ico|.*\\..*$).*)",
      "destination": "/"
    }
  ]
}

SANITY CHECK:
Verify json syntax formatting, run a quick 'npm run build' check, then commit and push directly to main branch.
