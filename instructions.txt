ACT AS: A Principal Web3 Full-Stack Architect and UI/UX Core Developer.
TASK: Implement Sonner toasts, fix the USDC gas decimal inflation, deploy live transaction activity logs, build token flip mechanics, and fully wire the Send, Swap, and Staking interactive panels.

CRITICAL ARCHITECTURAL SPECIFICATIONS:

1. INSTALL AND INITIALIZE SONNER NOTIFICATIONS:
   - Run 'npm install sonner' to download the modern notification library.
   - Open your master layout provider (src/app/layout.tsx or master wrapper file).
   - Import { Toaster } from 'sonner' and render the <Toaster position="bottom-right" theme="dark" richColors closeButton /> component at the base of the framework stack so alerts can be triggered globally.

2. FIX USDC GAS CARD DECIMAL PRECISION (src/app/page.tsx):
   - Locate the main dashboard grid card displaying "USDC GAS".
   - Modify the parsing logic: Native gas balances retrieved from the provider on Arc Testnet use standard 18-decimal scale format. Adjust your Viem 'formatUnits' function to parse this specific native balance using 18 decimals instead of 6 to completely eliminate the trillion-dollar card inflation.

3. LIVE ON-CHAIN ACTIVITY TRACKING PIPELINE (src/app/page.tsx):
   - Locate the "Recent Activity" component block. Strip out all hardcoded historical array values.
   - Use Viem's 'publicClient.getLogs' mechanism to poll the network every 8-10 seconds for real-time ERC-20 'Transfer' event logs emitted by the $DIBS smart contract address: 0x2b0ec237e5Cf460962E3eDe88cb676d83C807912.
   - Filter logs where either the 'from' or 'to' parameters match the logged-in user's wallet address ('user?.wallet?.address'). Map these parsed rows dynamically into your UI list:
     * User is sender: Method = "Transfer Sent", Status = "Confirmed".
     * User is receiver: Method = "Token Received", Status = "Confirmed".
     * Amount: Format the bigInt value with standard 18-decimal formatting.

4. WIRE THE TOKEN FLIP FLOP & SWAP BUTTON (src/app/page.tsx):
   - Wire the active toggle arrow button located between the Swap input modules to execute a function that flips the 'fromToken' and 'toToken' state selections.
   - Connect the "Execute Swap" button action:
     * If 'fromToken' is USDC: Trigger a live contract write using Wagmi/Viem to execute 'swapUsdcForDibs()' on the funded Vault address (0x3ed226184b4a00d1500e04f4fa89281107475597). Pass the user's typed USDC amount as the value parameter ('value: parseUnits(usdcAmount, 6)'). Wrap the transaction in a 'toast.promise()' statement tracking processing, completion, and gas error states.
     * If 'fromToken' is DIBS: Intercept the click event and instantly trigger a 'toast.error("DIBS to USDC liquidation is locked during the Testnet Alpha phase.")' message to protect the liquidity vault.

5. ACTIVATE THE SEND ASSET MODAL & TRANSACTION ENGINE:
   - Wire the main portfolio card's "Send" button to trigger an interactive glassmorphic overlay modal block.
   - Include a select dropdown for "USDC Gas" or "DibsCoin", a text input field for "Recipient Wallet Address (0x...)", and an "Amount" input.
   - Connecting the "Confirm Transfer" button action:
     * If sending USDC: Invoke walletClient.sendTransaction to route native gas tokens directly.
     * If sending DIBS: Run a contract write calling 'transfer(address,uint256)' on the main $DIBS contract address (0x2b0ec237e5Cf460962E3eDe88cb676d83C807912).
     * Encase the workflow within 'toast.promise()' status notifications.

6. ACTIVATE THE GOVERNANCE STAKING MODAL FRAMEWORK:
   - Connect the main dashboard's "Stake" action button to trigger an overlay modal capture container.
   - Allow users to enter an amount of $DIBS they want to lock up.
   - Wire the "Confirm Stake" submission button to a state updater that triggers a successful 'toast.success("Assets successfully committed to the Sovereign Staking Vault!")' notification, adds the input quantity to the dashboard's visual 'stakedBalance' tracker card, and closes the modal.

7. SYSTEM COMPILE AND DEPLOYMENT:
   - Run 'npm run build' to clean framework logs and confirm a 100% successful compile loop with zero TypeScript errors.
   - Run production delivery sequence: git add ., commit with message "feat: complete unified terminal engine with accurate metrics, live activity feeds, token flipping toggles, and asset transaction modals", and push origin main.

Execute this comprehensive full-stack interactivity and data wiring sweep immediately.
