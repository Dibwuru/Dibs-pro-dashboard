ACT AS: A Senior Web3 Protocol Architect.
TASK: Integrate Circle's App Kit SDK to activate the 'Send' modal, while strictly preserving all existing UI states, balance hooks, and dynamic routing logic.

CRITICAL PRESERVATION RULES (DO NOT DEVIATE):
1. UI STATE LOCK: Do NOT alter `src/app/page.tsx`, `Navbar.tsx`, or `Sidebar.tsx`. The unified `isUIActive` layout logic and the `handleHardDisconnect` functions are locked and working perfectly.
2. NETWORK & BALANCE LOCK: Do NOT modify any balance-fetching logic. USDC remains the native gas token on Arc Testnet. Do not introduce ERC-20 `balanceOf` checks for USDC.
3. ROUTING LOCK: Do NOT use `window.ethereum`. You MUST preserve the dynamic Privy provider extraction: `const provider = await activeWallet.getEthereumProvider();`.

IMPLEMENTATION STEPS (SEND MODAL ONLY):

1. INSTALL DEPENDENCIES:
   - Run: `npm install @circle-fin/app-kit @circle-fin/adapter-viem-v2 viem`

2. WIRE THE SEND COMPONENT (e.g., `src/components/SendModal.tsx`):
   - Import the App Kit and the Viem adapter.
   - On submit, retrieve the active provider from Privy's `useWallets()`.
   - Wrap the provider using the viem adapter configuration.
   - Instantiate the kit: `const kit = new AppKit();`

3. EXECUTE P2P TRANSFER:
   - Bind the user's destination address and amount inputs directly into the SDK's send method:
     ```typescript
     await kit.send({
       from: { adapter: viemAdapter, chain: "Arc_Testnet" },
       to: recipientAddressInput,
       amount: transferAmountInput,
       token: "USDC",
     });
     ```
   - Maintain our established UX standards: wrap the call in a `try/catch` block. Catch error code 4001 or "rejected" signatures to display a "Transaction Canceled" toast without freezing the UI. Trigger a success toast with the transaction hash upon completion.

4. DEPLOY:
   - Run `npm run build` to ensure type-safety across the new viem adapters.
   - Git commit with message: "feat: integrate circle app kit for p2p usdc sends and strictly preserve auth states" and push to main.
