ACT AS: A Lead Frontend Architect and Senior Web3 Core Systems Developer.
TASK: Clean up the main landing layout, clear out the naked wallet selection list, and simultaneously implement Navbar mobile optimization, Privy social options, and live Arc Testnet USDC gas tracking.

CRITICAL CODE MODIFICATION SPECS:
1. RESTORE PREMIUM LANDING (src/app/page.tsx):
   - Completely delete the vertical list of naked browser wallets (Injected, Phantom, MetaMask, etc.) from the center of the viewport.
   - Restore the premium analytics dashboard matrix container: render the master Portfolio Value metric card ($145,230.50 DIBS placeholder grid), the three child info grids (STAKED, EARNED, REWARDS), and the clean tabular layout grid for "Recent Activity".
   - If no wallet address is detected from either standard Wagmi extensions or Privy, wrap the dashboard canvas elements in a clean CSS blur filter ('blur-[4px]') and overlay a crisp, elegant, high-end centered text badge: "ARCTOR Terminal Locked. Use the command bar options above to connect your institutional wallet or sign in via email."

2. LIVE NATIVE USDC GAS TRACKER (src/components/Navbar.tsx):
   - Initialize a 'createPublicClient' instance from the 'viem' library pointing explicitly to the Arc Testnet RPC gateway: "https://rpc.testnet.arc.io" (Chain ID: 5042002).
   - Dynamically capture the active user string from either native wagmi useAccount or Privy usePrivy hooks. 
   - Write a background useEffect polling interval that triggers every 8 seconds to query the native token balance of that address. Convert the return value from formatEther/formatUnits and pipe it directly to update your custom Gas Status Navbar pill. Change the static placeholder string to render the exact dynamic layout: "Gas: {balance} USDC". If disconnected, fallback cleanly to "Gas: -- USDC".

3. PRIVY SOCIAL EXPANSION (src/providers/PrivyAuthProvider.tsx):
   - Update the loginMethods configuration parameter array to fully unlock additional premium onboarding tracks: loginMethods: ['email', 'google', 'twitter', 'apple'].

4. MOBILE RESPONSIVE FLEX SWEEP (src/components/Navbar.tsx):
   - Update the outer JSX layout wrapping wrappers for the Gas Pill, Theme Switcher, and authentication buttons using Tailwind breakpoints. Use 'hidden md:inline-flex' or elegant wrapping mechanics to prevent component smashing or design degradation on narrow screens.

5. COMPILATION AND REPOSITORY PIPE:
   - Run 'npm run build' to confirm a flawless Next.js 14 server build with zero configuration errors.
   - Execute production deployment: git add ., commit with message "refactor: clear naked wallet list landing page, enable live arc testnet usdc gas engine, expand privy socials and mobile optimization", and push origin main.

Execute this comprehensive terminal cleanup and data routing sequence now.
