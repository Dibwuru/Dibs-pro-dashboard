ACT AS: A Lead Web3 Smart Contract Integration Engineer and Full-Stack Architect.
TASK: Resolve the Wagmi "Connector not connected" runtime swap error by bridging Privy's wallet provider directly to Viem client actions, and implement local storage profile persistence.

CRITICAL REPAIR SPECIFICATIONS:

1. REFACTOR TRANSACTION SIGNING PIPELINE (src/app/page.tsx & src/app/swap/page.tsx):
   - ROOT CAUSE: Wagmi's 'useWriteContract' fails because it cannot detect Privy's embedded Google wallet connector natively without an explicit provider handshake.
   - THE FIX: Switch from Wagmi's 'useWriteContract' to Privy's native 'useWallets()' hook array to capture the active wallet interface.
   - IMPLEMENTATION LOGIC:
     ```typescript
     const { wallets } = useWallets();
     const activeWallet = wallets; // Captures the logged-in embedded or external wallet
     
     // When executing the transaction:
     const provider = await activeWallet.getProvider();
     const walletClient = createWalletClient({
       account: activeWallet.address as `0x${string}`,
       chain: arcTestnet, // Chain ID: 5042002
       transport: custom(provider)
     });
     
     // Trigger the write action directly on the client:
     const hash = await walletClient.writeContract({
       address: VAULT_ADDRESS, // 0x3ed226184b4a00d1500e04f4fa89281107475597
       abi: SWAP_VAULT_ABI,
       functionName: 'swapUsdcForDibs',
       value: parseUnits(usdcInput, 6)
     });
     ```
   - Bind this exact client write execution structure to BOTH the Swap page button and the Send asset modal to guarantee unified transaction execution. Wrap the entire sequence cleanly within your existing Sonner 'toast.promise()' status cycles.

2. PROFILE UPDATE PERSISTENCE LAYER (src/components/Sidebar.tsx):
   - Locate the Profile Bio/PFP editor modal state handlers.
   - Replace temporary React memory state variables with a persistent 'localStorage' synchronization utility.
   - On component mount, pull any saved credentials using 'localStorage.getItem("arctor_profile")'.
   - When a user saves their custom Display Name, Bio, or PFP URL details, save the structured JSON object directly using 'localStorage.setItem("arctor_profile", JSON.stringify(profileData))' so changes survive page refreshes and re-logins.

3. VERIFY AND DEPLOY:
   - Run 'npm run build' to clear compiler cache lines and verify 100% type safety with zero runtime or configuration export errors.
   - Execute production branch deployment: git add ., commit with message "fix: eliminate wagmi connector errors by wrapping privy providers natively via viem clients and add profile persistence", and push origin main.

Execute this comprehensive core web3 infrastructure repair now.
