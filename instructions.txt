ACT AS: A Senior Web3 Frontend Architect.
TASK: Completely overwrite the PrivyAuthProvider file to inject the correct production App ID and completely remove the Apple track.

CRITICAL HARDCODE SPECIFICATIONS:
1. TARGET FILE: src/providers/PrivyAuthProvider.tsx
2. FULL FILE OVERWRITE: Replace the entire contents of the file with this exact code block. Do not leave any old placeholder strings:

'use client';
import { PrivyProvider } from '@privy-io/react-auth';

export default function PrivyAuthProvider({ children }: { children: React.ReactNode }) {
  return (
    <PrivyProvider
      appId="cmpoxpjd900ie0dl86nwbv5hw"
      config={{
        loginMethods: ['email', 'google', 'twitter'],
        appearance: {
          theme: 'dark',
          accentColor: '#D4AF37',
          showWalletLoginFirst: false,
        },
        embeddedWallets: {
          createOnLogin: 'users-without-wallets',
        },
      }}
    >
      {children}
    </PrivyProvider>
  );
}

3. BUILD VALIDATION: Run 'npm run build' to clear out Next.js cache artifacts and verify compilation.
4. DEPLOYMENT PIPELINE: Execute git add ., commit with message "fix: hardcode production privy app id and entirely purge apple track", and push origin main.

Execute this strict file recreation and deployment loop immediately.
