ACT AS: A Senior Web3 Frontend Architect.
TASK: Implement a rock-solid, unified "Triple-Kill" logout flow across all components to eliminate ghost sessions completely.

CRITICAL PRESERVATION RULES:
- Do NOT alter, refactor, or touch the working 5-field userStakes tuple logic, event mapping, or staking reward formulas.
- Do NOT touch the newly implemented Privy-Wagmi Single Source of Truth architecture.
- Focus exclusively on sealing the disconnect execution loop.

IMPLEMENTATION CHANGES:

1. IMPLEMENT UNIFIED LOGOUT LOGIC (src/components/Web3Provider.tsx or shared auth hooks):
   - Create or update the global hard-disconnect sequence to strictly execute the following steps in order inside a try/catch block:
     a) KILL WAGMI: Invoke `disconnect()` from wagmi's `useDisconnect()`.
     b) KILL PRIVY: Invoke `logout()` from privy's `usePrivy()`.
     c) WIPE CACHE: Run `window.localStorage.clear();` and `window.sessionStorage.clear();`.
     d) HARD RESET: Run `window.location.reload();` to re-initialize the browser with a clean slate.

2. BIND TO ALL UI ELEMENTS:
   - Audit and apply this exact handler execution to the "Disconnect" click events in:
     - `src/components/Navbar.tsx`
     - `src/components/Sidebar.tsx`
     - The red disconnect action element inside the main dashboard portfolio metric block (`src/app/dashboard/page.tsx`).
   - Ensure any errors caught during the logout process do not freeze the UI; handle them gracefully while completing the storage wipe and page reload.

3. VERIFY:
   - Run 'npm run build' to confirm a flawless compilation with zero type mismatches.
   - Git commit with message: "fix: deploy unified triple-kill disconnect flow to wipe privy, wagmi, and storage state" and push to main.
