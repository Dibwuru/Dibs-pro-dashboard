ACT AS: A Senior Web3 Frontend Architect.
TASK: Mount the newly created SendModal component onto the main Dashboard view and wire up its trigger button.

CRITICAL PRESERVATION RULES:
- Do NOT touch the internal logic of 'src/components/SendModal.tsx' or the underlying App Kit integration. It is compiling perfectly.
- Retain all 'isUIActive', 'handleHardDisconnect', and native network balance states.

IMPLEMENTATION:
1. UPDATE DASHBOARD VIEW (src/app/page.tsx or the main authenticated layout):
   - Import the newly created 'SendModal' component at the top of the file.
   - Set up a simple React state to manage the modal's visibility: `const [isSendOpen, setIsSendOpen] = useState(false);`
   - Locate the 'Send' quick-action button inside your dashboard portfolio card. Attach an 'onClick' handler to flip this state: `onClick={() => setIsSendOpen(true)}`.
   - Render the `<SendModal isOpen={isSendOpen} onClose={() => setIsSendOpen(false)} />` element safely within the authenticated layout wrapper.

2. EXECUTE:
   - Run 'npm run build' to confirm the layout properties match the modal's export signatures.
   - Git commit with message: "feat: connect SendModal trigger to main dashboard layout" and push to main.
