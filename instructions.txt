ACT AS: A Senior Web3 Frontend Architect and Smart Contract Integration Specialist.
TASK: Sync the Privy wallet connection state with the UI display and implement strict frontend validation to prevent gas-burning transaction reverts.

CRITICAL DISCONNECT FIXES:

1. WALLET STATE SYNCING (Navbar.tsx, Sidebar.tsx, & Layouts):
   - Modify the layout components to read the 'wallets' array from Privy's 'useWallets()' hook.
   - If 'wallets.length > 0', grab 'wallets[0].address', truncate it (e.g., 0x1234...5678), and display it inside the button instead of showing "Connect Wallet".
   - Treat an active wallet in the 'wallets' array as a fully valid connected session so the user can interact with the swap/stake panels immediately without requiring a separate email login.

2. FIX SWAP EXECUTOR & PREVENT GAS DRAINING (src/app/swap/page.tsx):
   - VALUE PAYLOAD FIX: When executing the 'swapUsdcForDibs' transaction via Viem, ensure the user's input amount is explicitly passed inside the native 'value' field of the contract write call: 'value: parseUnits(swapInput, 18)'.
   - LIQUIDITY CHECK: Before allowing a user to click "Swap", use a Viem public client read call to check the vault's active $DIBS balance: 'dibsToken.balanceOf(vaultAddress)'. 
   - DEFENSIVE UI: If the vault's $DIBS balance is less than the expected 'dibsAmount' output, disable the swap button and display a red warning text: "Transaction blocked: Vault has insufficient $DIBS liquidity."
   - STRICT ERROR TOASTS: Fix the transaction toast wrappers. If 'waitForTransactionReceipt' returns a reverted status or throws an RPC exception, display an explicit red 'toast.error("Transaction Failed/Reverted")' instead of a successful confirmation alert.

3. BUILD & PRODUCTION DEPLOY:
   - Run 'npm run build' to confirm clean type-checking and compilation.
   - Git commit with message: "fix: sync Privy wallet connection state to UI and add defensive vault liquidity validation for swaps" and push to main.O
