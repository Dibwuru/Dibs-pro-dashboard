ACT AS: A Lead Web3 Core Systems Engineer and Full-Stack Architect.
TASK: Completely remove Wagmi from the swap and send execution paths to eliminate the '@wagmi/core' chain mismatch error, and configure the custom chain inside the Privy provider framework.

CRITICAL ARCHITECTURAL REPAIRS:

1. CONFIGURE CUSTOM CHAIN IN PRIVY PROVIDER (src/app/layout.tsx or master provider configuration file):
   - Locate the <PrivyProvider> component wrapper.
   - Define your custom Arc Testnet chain object explicitly:
```typescript
     const arcTestnet = {
       id: 5042002,
       name: 'Arc Testnet',
       network: 'arc-testnet',
       nativeCurrency: { name: 'USDC', symbol: 'USDC', decimals: 18 },
       rpcUrls: { default: { http: ['YOUR_ARC_RPC_URL'] }, public: { http: ['YOUR_ARC_RPC_URL'] } }
     };
     ```
   - Pass this chain directly into Privy's initialization configuration: `config={{ supportedChains: [arcTestnet], embeddedWallets: { createOnLogin: 'users-without-wallets' } }}`. This ensures Privy natively recognizes the network for chain-switching prompts.

2. TOTAL PURGE OF WAGMI WRITE HOOKS (src/app/swap/page.tsx & src/app/page.tsx):
   - Locate your transaction handlers: 'handleSwapExecute', 'handleSendConfirm', and 'handleSwap'.
   - COMPLETELY REMOVE 'useWriteContract' or any other transaction execution hooks imported from 'wagmi' or '@wagmi/core'.
   - IMPLEMENT CLEAN STANDALONE VIEM WRITES:
     * Pull the active wallet array using Privy's 'useWallets()'.
     * Run the explicit asynchronous chain-switching check against the embedded provider:
```typescript
       const { wallets } = useWallets();
       const activeWallet = wallets[0];
       if (activeWallet) {
         const currentChainId = Number(activeWallet.chainId.replace('eip155:', ''));
         if (currentChainId !== 5042002) {
           await activeWallet.switchChain(5042002);
         }
         
         // Build a PURE Viem client completely independent of Wagmi:
         const provider = await activeWallet.getProvider();
         const walletClient = createWalletClient({
           account: activeWallet.address as `0x${string}`,
           chain: arcTestnet,
           transport: custom(provider)
         });
         
         // Trigger execution directly via the standalone Viem wallet client:
         const hash = await walletClient.writeContract({
           address: '0x3ed226184b4a00d1500e04f4fa89281107475597', // Vault Address
           abi: SWAP_VAULT_ABI,
           functionName: 'swapUsdcForDibs',
           value: parseUnits(usdcInput, 6)
         });
       }
       ```
- Re-wrap this clean Viem pipeline inside your existing Sonner 'toast.promise()' notifications. Because Wagmi is no longer involved in the write path, the `@wagmi/core` error is completely avoided.

3. PRODUCTION SYSTEM COMPILE AND SHIP:
- Run 'npm run build' to clear out Next.js framework build layers and confirm 100% compilation safety with zero TypeScript errors.
- Deploy code updates: git add ., commit with message "fix: completely remove wagmi from execution path to fix core chain mismatch errors", and push origin main.

Execute this full-stack code isolation loop immediately.
