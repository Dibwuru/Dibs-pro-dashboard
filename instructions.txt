ACT AS: A Senior Web3 Frontend Architect.
TASK: Add an external link to the Circle USDC Faucet inside the Sidebar navigation layout.

CRITICAL PRESERVATION RULES:
- Do NOT alter, refactor, or disrupt the newly deployed "Triple-Kill" disconnect handlers or the Privy-Wagmi architecture.
- Focus strictly on a localized UI addition inside the Sidebar component.

IMPLEMENTATION:
1. UPDATE SIDEBAR COMPONENT (src/components/Sidebar.tsx or your specific navigation layout file):
   - Locate the navigation container inside the sidebar layout.
   - Add a new navigation list item or anchor link pointing to: `https://faucet.circle.com/`
   - Enforce explicit security and window attributes: `target="_blank" rel="noopener noreferrer"`
   - Style the item to perfectly match your theme (dark/gold navigation design system). 
   - Use clear text like "Get Test USDC" or "Circle Faucet" alongside a clean external-link icon.

2. VERIFY:
   - Run 'npm run build' to confirm no broken tags or missing icon imports throw type errors.
   - Git commit with message: "feat: add external Circle faucet link to sidebar navigation" and push to main.
