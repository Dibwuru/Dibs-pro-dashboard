ACT AS: A Senior Web3 Frontend Architect and Mobile UI/UX Engineer.
TASK: Resolve the infinite loading toast via Viem block receipt confirmations, trigger live state refreshes, wire the token flip arrow logic, and guarantee strict mobile/desktop parity.

CRITICAL ARCHITECTURAL REPAIRS:

1. IMPLEMENT VIEM TRANSACTION RECEIPT WAITING (src/app/page.tsx & src/app/swap/page.tsx):
   - Locate the standalone Viem 'walletClient.writeContract' execution paths in BOTH your Swap and Send handlers.
   - Fix the infinite toast hang by instantiating a standalone public client directly beneath your wallet client setup:
```typescript
     import { createPublicClient, custom } from 'viem';
     
     const publicClient = createPublicClient({
       chain: arcTestnet,
       transport: custom(provider)
     });
     
     const hash = await walletClient.writeContract({ ... });
     
     // WAIT for the blockchain to mine the block before advancing:
     const receipt = await publicClient.waitForTransactionReceipt({ hash });
     
     if (receipt.status !== 'success') throw new Error('Transaction reverted on-chain');
     ```

2. TRIGGER FRONTEND STATE REFRESH:
   - Immediately after 'waitForTransactionReceipt' confirms successfully, and BEFORE resolving the Sonner promise toast, invoke the component's data-fetching routines (re-running the read states for 'dibsBalance' and 'gasBalance').
   - This guarantees the user's dashboard numbers update instantaneously on both mobile and desktop screens the moment a block confirms.

3. WIRE THE TOKEN FLIP ARROWS ENGINE:
   - Locate the toggle arrow button positioned between the "You Pay" and "You Receive" input fields in the Swap interface.
   - Implement a state toggle function 'handleTokenFlip': Clicking the arrow must cleanly swap the 'fromToken' and 'toToken' state variables and their respective input values.
   - SWAP EXECUTION GUARD: If 'fromToken' is flipped to DIBS, intercept the "Execute Swap" click trigger and immediately return 'toast.error("DIBS to USDC liquidation is locked during the Testnet Alpha phase.")' to prevent contract reverts.

4. CROSS-PLATFORM TOUCH & RESPONSIVE MANDATE:
   - Ensure the new Token Flip Arrow button natively supports mobile touch interactions by adding 'cursor-pointer select-none active:scale-95 p-2' utilities.
   - Verify that this interaction, along with the 'Execute Swap' button and toast notifications, are fully accessible on viewport widths under 768px without being intercepted by invisible flex or grid containers. 
   - Ensure the UI logic seamlessly syncs whether triggered from the desktop sidebar layout or the mobile BottomNav view.

5. PRODUCTION VERIFICATION:
   - Run 'npm run build' to confirm 100% compilation safety with zero missing Viem imports or type mismatches.
   - Deploy code updates: git add ., commit with message "fix: implement cross-platform publicClient receipt waiting, instant state refreshing, and touch-optimized token flip interactions", and push origin main.

Execute this precise synchronization and interaction loop immediately.
