ACT AS: Principal Web3 Systems Architect
TASK: Initialize the Phase 2 infrastructure stack by injecting cross-chain deposit packages and establishing an isolated Agent Automation view route.

GUARDRAILS:
- Do NOT touch or refactor any components verified by our test runner in 'src/__tests__/'. Maintain absolute code isolation.

IMPLEMENTATION CRITERIA:
1. Package Injection:
   - Run 'npm install @lifi/sdk @lifi/sdk-provider-ethereum' to embed the necessary cross-chain swap-and-bridge developer kits.
2. Build Agent Analytics View:
   - Create a brand new folder route at 'src/app/agent/'.
   - Add a clean, production-ready 'src/app/agent/page.tsx' layout. 
   - Design a premium dark-mode dashboard interface for this page that features:
     * An "Agent Identity Registration (ERC-8004)" profile card module displaying an avatar placeholder, code verification hashes, and status.
     * A "Programmable Workflow Controls (SpendingLimiter.vy)" container visualizing current spending velocity metrics (e.g., "Current Velocity: 0.00 / 500.00 USDC" with a simulated progress tracker bar).
     * A "Cross-Chain Onboarding Bridge" input form panel designed to collect target parameters for cross-chain liquidity paths via LI.FI.

VALIDATION:
- Run 'npm test' to ensure the existing 30 assertions continue to pass perfectly.
- Run 'npm run build' to confirm successful static compilation of the new route. Commit and push directly to main.
