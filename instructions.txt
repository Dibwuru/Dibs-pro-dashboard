ACT AS: A Senior Web3 Frontend Architect and Code Merge Specialist.
TASK: Carefully merge new UI/UX designs from the /root/friend-ui directory into the current production codebase WITHOUT breaking the existing Viem/Privy transaction logic.

CRITICAL MERGE PROTOCOL:
1. READ THE NEW UI FILES:
   - Analyze the updated frontend files located directly in the '/root/friend-ui' directory. Pay special attention to tailwind.config.ts, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, and any updated components in the src/components/ folder.
   
2. SURGICAL EXTRACTION (VISUALS ONLY):
   - Extract the new Tailwind CSS classes, HTML structural changes, color palettes, animations, and typography implemented by the UI designer.
   - Apply these visual updates to our current production files in '/root/dibs-pro/src/' and update the root configuration files (like tailwind.config.ts) if the designer added new custom theme colors or fonts.

3. PRESERVE CORE WEB3 LOGIC (DO NOT OVERWRITE):
   - You MUST absolutely preserve our current Privy 'useWallets()' logic.
   - You MUST absolutely preserve our standalone Viem 'createWalletClient' and 'createPublicClient' execution paths.
   - You MUST absolutely preserve our 'waitForTransactionReceipt' synchronization and state-refreshing logic.
   - DO NOT re-introduce any Wagmi hooks ('useWriteContract').
   
4. PRODUCTION VERIFICATION:
   - Run 'npm run build' to ensure the newly merged UI code compiles flawlessly with our established Web3 backend logic.
   - Push to production: git add ., commit with message "ui: merge updated designer frontend aesthetics from friend-ui while preserving core web3 transaction pipelines", and push origin main.
