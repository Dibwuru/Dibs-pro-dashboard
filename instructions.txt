ACT AS: A Senior Web3 Frontend Architect and UX Specialist.
TASK: Fix the user authentication state UI, implement a unified disconnect flow, and correct the USDC balance reading on the swap page.

CRITICAL UX REPAIRS:

1. GLOBAL DISCONNECT FUNCTIONALITY (Navbar & Sidebar):
   - Import 'usePrivy' (for logout) and extract the 'disconnect' method from the connected wallet via 'useWallets()'.
   - Wherever the connected wallet address pill is displayed, add an explicit "Disconnect" button or icon next to it.
   - When clicked, execute both 'logout()' AND 'wallets[0].disconnect()' to ensure the session is completely and cleanly wiped.

2. LANDING PAGE STATE FIX (src/app/page.tsx):
   - Read the connection state: if 'wallets.length > 0' or 'authenticated' is true, DO NOT show the "Connect Wallet" or "Sign In with Email" buttons in the central hero card.
   - Replace them dynamically with a "Go to Swap Terminal" action button that routes to the '/swap' page, and a secondary "Disconnect" button.

3. FIX USDC BALANCE BLINDNESS (src/app/swap/page.tsx):
   - The Arc Testnet natively uses USDC as its gas token. USDC is NOT an ERC20 token on this network.
   - Remove any ERC20 'balanceOf' checks for USDC on the frontend.
   - Import and use viem's publicClient 'getBalance({ address: userAddress })' (or wagmi's useBalance) to fetch the user's native token balance.
   - Display this native balance as the user's available USDC balance on the Swap UI, and use it for the "Insufficient balance" validation check before allowing a native payable swap.

4. BUILD & DEPLOY:
   - Run 'npm run build' to confirm clean compilation.
   - Git commit with message: "fix: resolve auth UI state, add global disconnect, and map USDC to native balance" and push to main.
